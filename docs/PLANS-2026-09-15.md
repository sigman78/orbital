# Plans, 15 September 2026

Gathered from the September sessions after the lens, HDR, exposure, timing,
memory and freeze-culling work merged. Two horizons: the immediate list is
small, self-contained work that can go in any order as time allows; the long
horizon is the direction the belt, the bodies and the frame loop are heading,
where the pieces have to fit together and the order matters. Decisions behind
the items are in DECISIONS.md, measurements in PERFORMANCE.md and
docs/performance/.

## Immediate

Simple enough to pick up without further design. Each is a branch and a PR.

| # | Task | Notes | Effort |
|---|------|-------|--------|
| I.1 | Done (PR #24, 15 Sep). Exponential moving average for every panel reading | Replace the 0.5 s deque window in `timing_average.hpp` with an EMA that covers the group timings, their children, the meter, cull counts and memory. Time constant about 0.5 s, reset on camera cut and bookmark. | half a day |
| I.2 | Done (PR #25, 15 Sep). CPU frustum culling of bodies | Sphere against the padded frustum, same parameters the belt cull takes; uses the frozen cull camera when the freeze holds. Prerequisite for the body work below. | half a day |
| I.3 | Done (PR #26, 15 Sep; the increases do not reproduce, same-session runs of both revisions). Confirming run for the fullscreen belt surface and atmosphere increase | A +0.45 ms delta was flagged once; the child timings now say which pass moved. Two benchmark repeats at 1920x1080, recorded under docs/performance/. | hour |
| I.4 | Done (PR #27, 15 Sep; the key against a whole-frame mean, not the band; tools/check-exposure.py). Mars at +2.4 stops under the histogram meter | Confirm whether the highlight band or the key is responsible; a per-body sanity capture in the suite would catch the next one. | hours |
| I.5 | Done (PR #28, 15 Sep; metadata path confirmed, peak 465 nits and SDR white 388 reported, headroom 1.2; the shoulder on the panel is still a live look). HDR output on the display | Re-check the metadata path and paper-white tone on the HDR desktop after the exposure changes; record the observed peak. | hours |
| I.6 | Done (PR #29, 15 Sep; motion history weight 0.8 to 0.6, most of the flight deficit is the jittered reconstruction). TAA contrast loss | TAA-only light was halved in the belt flight captures; check the history weight and clip against the Sep 11 belt and Dawn medians. A stopgap only: the real fix is motion vectors (L.3). | half a day |
| I.7 | Dropped (15 Sep; PR #30 closed, not needed). Frozen frustum outline | Optional: draw the frozen cull camera's frustum edges while Freeze culling is on, so the cut is visible from outside. | half a day |

## Long horizon

Where the renderer is going. Three threads that meet in the middle: the belt
moves to CPU-owned state so physics can drive it, the bodies get the same
visibility and level treatment the belt already has, and the frame loop is
rearranged so culling and state production run ahead of rendering. The
threads share data structures, so the order below keeps each step small and
measurable.

### Vision

- The CPU owns the state of everything that moves: bodies and rocks in one
  structure-of-arrays store with the same integrator interface, bodies being a
  few entries with mass and their own draw path. Rocks can be spawned and
  destroyed. Motion can be anything the physics wants; the renderer only reads
  positions and previous positions.
- The GPU is the fine stage: it refines visibility per rock, chooses levels,
  lights the splats and draws. It never recomputes motion.
- Every drawn thing carries its motion, so temporal AA reprojects per pixel and
  needs no clipping tricks to hide moving rocks.
- Visibility is one stage with two granularities: the CPU rejects bodies and
  belt sectors from its own state, the GPU refines rocks. Both read one
  frustum, one occluder set, one cull camera, which the freeze switch can hold.
- Bodies are drawn at the cost their size deserves: a depth pre-pass so overlap
  is free, shader tiers that fade detail terms without changing lighting, and
  impostor discs below a few pixels.
- Culling and state production run one frame ahead of rendering, so the frame's
  critical path is the draw.

### Thread A: belt motion on the CPU

| # | Step | Notes | Effort |
|---|------|-------|--------|
| L.1 | Done (16 Sep; float4 per rock in two device slices, the other slice is the previous frame; 0.7 ms CPU per frame at 280k rocks single-threaded, GPU unchanged; see DECISIONS). Per-rock state buffer written by the CPU | SoA position and previous position; the static record keeps radius, seeds and class. The cull, light-map splat and disc bake read state instead of recomputing the closed form. Staging copy into device memory per frame, not host-visible reads across the bus (the BAR heap is at its edge). First cut writes the same closed-form motion, so the image is unchanged and the upload cost is measured honestly (12.5 MB per frame at 520k rocks). | one day |
| L.2 | Done (16 Sep; scene/belt_motion.cpp owns the rocks and core/jobs.hpp the workers; the state is float4 (x, y, z, phase) by choice, the sweep 0.05 ms on seven workers at 280k, the 4.5 MB host-visible write 0.7 ms whatever the thread count; sector flags wait for L.4, the far tail is stepped every frame for motion vectors; see DECISIONS). Integration pass with SIMD and jobs | One sequential streaming pass per frame that integrates, writes the state and sets sector flags; SIMD lanes, a job split across cores, the far tail optionally across frames. Deterministic fixed step so captures and benchmarks stay reproducible. The data does not fit in cache at this count; the pass is bandwidth bound, so everything happens in the one sweep. | two days |
| L.3 | Done (16 Sep; a motion pass at equal depth into an RG16F target, previous centre and rotation through the instances, the sky filled; parity with the depth-reconstructed reprojection for camera motion, the gain is for what moves; see DECISIONS). Motion vectors for rocks and bodies | Previous position through the instance record to a velocity target, per-pixel reprojection in TAA. Replaces the moving-clip workaround; bodies get rotation motion for free. | one to two days |
| L.4 | Sector partition and coarse CPU cull | Belt sorted by band and angular sector with slack for spawns; the CPU rejects sectors in bulk, the GPU refines. Per-rock CPU culling stays out: the GPU pass is 0.19 ms and the splat lighting needs textures. | one day |
| L.5 | Unified state model with physics | Bodies and rocks in the same store and integrator; spawn and destroy as appends and holes; the physics model of the user's choosing on top. | open |

### Thread B: bodies at the cost their size deserves

| # | Step | Notes | Effort |
|---|------|-------|--------|
| L.6 | Done (PR #25, 15 Sep; sky drawn last and back-face culling with it; rock pre-pass tried and dropped). Body depth pre-pass | Order independent, so a giant in front of its belt or another body costs nothing to shade twice. Measured with the Bodies and Rocks children before and after; draw-order tricks were rejected because the occluder set changes with the view. Needs I.2. | one day |
| L.7 | Earth step done (15 Sep: per-draw detail weight, cloud shell folded into the ground below 80 px, the zoom shimmer reproduced and fixed with a depth bias on the shell); giant and airless next; the atmosphere pass is bounded to the projected shell (15 Sep), its step tiers tried and dropped. Fast-path body shader tiers | Chosen by projected size. No-pop rule: lighting and tone terms stay identical across tiers, only detail terms (normal maps, clouds, night lights, ocean specular, band detail) fade with the texel-to-pixel ratio, so the switch is invisible. Earth first, then the gas giant, then airless bodies. Earth's first step: below a projected size the cloud shell is not drawn and the cloud map is blended into the ground shading in the surface shader; the shell itself keeps a depth bias so it never fights the ground at distance. This is also the fix for the shimmer seen on a distant Earth under the telescope zoom: the shell sits 0.9 percent above the ground and, with the standard float depth over 0.02 to 2000 units, the two are within a couple of depth steps beyond about 45 units, so the shell's depth test flips per pixel; the zoom makes the disc large without changing depth precision. Reproduce first (far zoomed view, two consecutive frames, flipping pixels with the shell on and off), then pick the switch size where the two paths agree within the dither. | two days |
| L.8 | Far-tier body impostors | Below a few pixels a body is a disc of its average colour with the phase shading, drawn as a billboard; no surface pass at all. The smallest tier of L.7. | one day |

### Thread C: the frame loop

| # | Step | Notes | Effort |
|---|------|-------|--------|
| L.9 | (16 Sep: built on branch render/async-cull and measured a wash: the cull is 0.25 ms of GPU time wherever it runs and the frame is GPU-bound, so standing views gain 0.15 ms and moving views lose 0.1 to 0.2 ms to the widened frustum. Not merged; revisit only when the cull's output gets a CPU consumer. A CPU frustum pass ahead of the GPU cull (branch render/cpu-cull) was measured pointless too: the belt fills the review views and the size cut-off keeps sub-pixel rocks for the splats, so 63 to 100 percent of the population are candidates and the GPU cull's 0.25 ms does not move; the per-frame cost is the population. The rock state write already runs before the wait for the previous frame, in two staging slots by parity; see DECISIONS.) Async cull one frame ahead | Double-buffered cull scratch, frustum padded for one frame of motion, camera cuts fall back to a same-frame cull. With L.1 the CPU produces next frame's state and sector list together. After L.6 to L.8 so its padding covers the body paths too. | one day |
| L.11 | (17 Sep: built behind a toggle on branch render/speckle: `--point-cutoff px --speckle 1`, the panel's slider and checkbox. Within 10 percent of the sand it replaces; +1 ms on the giant view at a 1 px cut-off, to be judged by eye. Judged: invisible by design, not what was wanted. The twinkle veil followed on the same branch: `--veil 1`, the panel's sliders; sparse sunward twinkles and a glow over the whole belt, +1 ms.) Analytic sub-pixel rocks | The near march stands in for the rocks under the point cut-off with a world-anchored point field, so the cut-off can rise and the sub-pixel population leave the per-frame path. | one day |
| L.10 | Reversed-Z depth | Deferred: near maps to 1 and far to 0 so float depth keeps near-uniform relative precision over the range; the general fix for thin layers at distance (cloud shell, atmosphere depth reconstruction). Touches the projection, clear value, compare ops, the sky's far-plane test in the TAA resolve and the linear depth helpers, and may reach the belt (splat mask depth, disc) and the shadow paths in ways not predicted, so it waits until the body tiers are settled and gets its own capture diffs across every bookmark. | one day |

### Order across threads

1. I.1 and I.2, so readings are legible and bodies are culled before anything is measured.
2. L.6 with its confirming run (I.3 alongside).
3. L.1 then L.3, since motion vectors also settle I.6.
4. L.7 and L.8 together.
5. L.9 once the body paths are settled.
6. L.2, L.4 and L.5 when the physics model is ready; L.1 already gives it the buffer to write into.

### Thread E: the planetoid

A generated body for close views, the first of the procedural planets in IDEAS.md.

| # | Step | Notes | Effort |
|---|------|-------|--------|
| P.1 | Done (17 Sep, branch planetoid/body). The body | Planetoid class, showcase role, `scene/terrain.hpp` as the one source of height and albedo, start-up bake into the airless shader's map layout, bookmark 9, check shot. | one day |
| P.2 | Cube-sphere near tier | Six faces, a quadtree of patches per face subdivided by projected size with hysteresis, patches generated on the CPU from `PlanetoidTerrain` (positions, normals) into a streamed device pool with an LRU and a staging ring, skirts at level seams, drawn through the surface path with a patch mode; switches on above the size where the finest sphere level runs out. | three days |
| P.3 | Terrain features | Layered detail per patch level (rising noise octaves, small craters), a material function beside the height (slope and altitude classes), and per-patch tiles when the per-vertex evaluation gets too coarse. | open |

### Thread D: the shader library

From SHADER_LIBRARY_REVIEW.md (16 Sep), checked against the source the same
day. Every finding below reproduced except where the note says otherwise. The
small fixes are one PR each; behaviour-changing patches stay separate from
extraction patches so capture diffs remain attributable.

Housekeeping, confirmed defects and drift:

| # | Step | Notes | Effort |
|---|------|-------|--------|
| S.1 | Done (PR #42, 16 Sep). Cloud geometry include | `planets/cloud_geometry.slang` uses `ORBITAL_CLOUD_HEIGHT` without including `scene_shared.h`; it compiles in production only because every entry point includes the header first, and `tools/check-shader-helpers.py` fails on it. Include the header directly. Do not fix it in the checker. | minutes |
| S.2 | Done (PR #42, 16 Sep). Helper checker reports every failure; shader job in CI | The checker stops at the first `CalledProcessError`; collect failures and exit non-zero at the end. CI runs formatting and CPU-only builds, no shader step: add a job that downloads the pinned slang (2026.14.1, same URL and hash as `tools/bootstrap.ps1`) and spirv-val, builds `orbital_shaders` with validation on, and runs the helper check. Neither needs a device. | half a day |
| S.3 | Done (PR #42, 16 Sep). FOUNDATION.md ABI numbers | The doc says 40 textures in one place and 51 in another; `ORBITAL_TEXTURE_COUNT` is 53. It says a 32-byte push root; `sizeof(Root)` is 40. Its "reflection verifies" sentence describes a review-time check, not code in the tree (no reflection call in src/); reword it, or point it at S.14 once that exists. | hour |
| S.4 | Done (PR #42, 16 Sep). Named pass modes for belt blur and cull | `belt/beltblur.slang` tests `root.mode == 0` and `belt/cull.slang` tests `root.pass` against 0, 1 and 2 with only a comment for meaning. Give each a small shared header next to its C++ recorder, the way `post/bloom_shared.h` does. SMAA and dust already have local named constants; share them only when the C++ side needs the names. `root.pass` also carries a caster count in the splat shaders, which is a separate field in all but name. | hour |
| S.9 | Done (PR #42, 16 Sep). One background-depth helper | `d >= .999999` appears in `scene/view.slang`, `aa/temporal.slang`, `post/sun_occlusion.slang` and `belt/beltfar.slang`. One `isBackgroundDepth` in `lib/projection.slang`, same value, so the semantics move in one place when L.10 changes the depth scheme. With near 0.02 and far 2000 the threshold is crossed at 1,818 units, so any geometry beyond that already reads as sky; nothing in the showcase sits there today. | hour |

Correctness fixes, each with a capture before and after:

| # | Step | Notes | Effort |
|---|------|-------|--------|
| S.5 | Done (PR #42, 16 Sep). Giant detail gradients before the branch | `giantDetail` returns on `fine <= 0` before `sampleSphere` takes `ddx`/`ddy`; derivatives inside divergent control flow are undefined in Vulkan. Compute the seam-correct gradients at the top of the fragment and pass them through a `SampleGrad` path. The image should not change; run the giant checks to confirm. | hours |
| S.6 | Done (PR #42, 16 Sep). Solar occlusion bounded to the light segment | `solarVisibility` accepts any occluder with `t > 0`, including one beyond the sun. Add the upper bound and a fixture with the review's counterexample (point at origin, sun at 10, body at 20). No showcase image changes, the sun is farther than every body. The penumbra is `max(t * 0.007, 0.015)`, distance scaled with a floor, not fixed; deriving it from the sun's angular radius is optional. | hour |
| S.7 | Done (PR #42, 16 Sep). Exact forward shell intersection | `cloudShellDirection` offsets the ray by `rise / max(N.L, .25)`; at grazing sun that gives 0.036 radii where the sphere intersection gives 0.134. `cloudShellView` already has the exact form; share it, define the no-hit case. Visible at the terminator cloud shadow, so a live review of the terminator and limb views. | half a day |
| S.8 | Done (PR #42, 16 Sep; no change in the six Moon and Mars check views, a polar shot under grazing light is wanted). Latitude-correct relief shadow step | `reliefShadow` steps one texel arc of 2 pi / 4096 at every latitude; on the equirectangular map the longitude texel shrinks by cos(latitude), 17 percent of the equator arc at 80 degrees, so high-latitude shadows are traced too short. Divide the longitude step by cos(latitude), define the pole and seam behaviour. Moon and Mars polar captures. | half a day |

Structure, only when the procedural planets in IDEAS.md are picked up. The
review's case is that one body of each family is hard-wired through
`Frame`, `resource_slots.h`, `Showcase::resolve` and
`renderer_resources.cpp` (`belts.front()`), so a second giant or belt cannot
have its own maps or parameters. That is confirmed and is the real
prerequisite; a shader graph, bindless descriptors or Slang modules are not.

| # | Step | Notes | Effort |
|---|------|-------|--------|
| S.10 | Done (PR #43, 16 Sep; matrices stay out of the records, an unused matrix load in the per-rock decode cost the cull pass 0.5 to 1 ms). Typed accessors over Frame | Shader-side decoders that read the packed `Frame` into view, light, body and belt records without changing the layout. ABI, pipeline names and captures unchanged. Can go in any time; makes S.11 a smaller diff. | one day |
| S.11 | Per-body material records | A body index per draw and a material record per body holding exactly today's settings, texture slots included. Airless first: Moon and Mars are already two bodies of one family distinguished by role in `surface_airless.slang` (`MOON_HEIGHT_RANGE`, `MARS_HEIGHT_RANGE`), so they are the acceptance scene. Then the giant. `Showcase::resolve` becomes the adapter that fills the records. Texture dimensions, height ranges and flow units move from shader literals into the record; keep the CPU clamps (the giant cycle floor at 0.1 s). | three to four days |
| S.12 | Surface sample and lighting split | `Material` carries `water` and `visibility`; `shadeSurface` mixes BRDF, ocean tuning, derivative roughness filtering and a fixed fill colour. Split into a surface sample, a lighting context and a fragment adapter that owns the derivatives, with the ocean as a named policy. Numerically identical through the adapter first; model changes later and separate. | two days |
| S.13 | Per-belt and per-body media resources | Belt maps, bounds and invalidation per belt instead of `belts.front()`; atmosphere profile per body. Shares data structures with L.1 and L.5, so it is ordered with them. | open |
| S.14 | Executable ABI check | Slang reflection of `Frame`, `Root` and the packed rock records against `gpu_types.hpp` offsets, plus descriptor count and push range, run offline in the shader CI job. Replaces the FOUNDATION.md prose in S.3. | one day |
| S.15 | Shader contract notes | The review's table of coordinate, depth, temporal, colour, alpha and stage conventions as a short docs page, with the preconditions repeated at the public helper definitions. The four `float4` outputs that must stay distinct (TAA history, splat metadata, far belt, atmosphere) are the first entries. | hours |

Not taken up: RGB transmittance for the atmosphere (scalar alpha with the
0.94 cap is a deliberate model and nothing needs a thick or coloured medium
yet), Slang modules and `ParameterBlock`, bindless resource indexing, and a
general shader graph. Revisit the first when a second atmosphere exists.

Order: S.1 to S.4 and S.9 in one or two PRs; S.5 to S.8 as four capture-reviewed
PRs in any order; S.10 and S.15 when convenient; S.11 to S.14 with the
procedural-planet work, after L.1 and L.7 so the body records and the belt
state land on the same structures.
